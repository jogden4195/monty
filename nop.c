#include "monty.h"

/**
 * np - fuction thst does nothing
 * @stack: pointer to head of stack
 * @line: current line number
 */
void nop(stack_t **stack, unsigned int line)
{
	(void)*stack;
	(void)line;
}
